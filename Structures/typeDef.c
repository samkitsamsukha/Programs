#include <stdio.h>

typedef struct employee{
    int salary;
    int id;
} emp;

#define PI 3.142

int main(){
    emp e1, e2;
    e1. salary = 14000;
    e1.id = 1;
    e2. salary = 16000;
    e2.id = 2;
    printf("Salary of first employee: %d\n", e1.salary);
    printf("ID of first employee: %d\n", e1.id);
    printf("Salary of second employee: %d\n", e2.salary);
    printf("ID of second employee: %d\n", e2.id);
    printf("%f", PI);
    return 0;
}